# Încărcat automat doar la `vite build` (nu la `vite` / npm run dev).
# Dev local folosește proxy /api → localhost:4001 fără variabile aici.
VITE_VECINDARIO_API_URL=https://api.vecindario.decaminoservicios.com
VITE_PUBLIC_APP_ORIGIN=https://vecindario.decaminoservicios.com
